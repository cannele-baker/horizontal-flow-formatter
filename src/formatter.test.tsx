import React from "react";
import { render, unmountComponentAtNode } from "react-dom";

import { createNode2dArr } from "./formatter";


// test('basic', async () => {
//     expect(sum()).toBe(0);
// });
